---
sidebar_position: 1
title: Introduction
slug: /
---

# API TrustSend — Intégration Business

<div className="ts-lead">
Encaissez, payez et suivez l'argent de vos clients depuis votre propre backend : mobile money,
wallets multi-devises, cartes et webhooks, derrière une seule clé API.
</div>

<div className="ts-endpoint">
  <span className="ts-endpoint__label">Base</span>
  <span className="ts-endpoint__url">{'https://api.trustsend.africa/api/v1'}</span>
</div>

Cette documentation s'adresse aux **développeurs intégrant un compte business TrustSend**. Elle ne couvre que les endpoints accessibles à un compte business — pas les APIs internes réservées aux utilisateurs finaux de l'application ou à l'équipe TrustSend.

## Par où commencer

<ul className="ts-steps">
  <li>
    <a href="/authentication/business-auth">
      <strong>Authentification</strong>
      <span>Créer le compte, obtenir une clé API ou un token de session.</span>
    </a>
  </li>
  <li>
    <a href="/kyc/submitting-documents">
      <strong>KYC</strong>
      <span>Soumettre les documents requis pour activer le compte.</span>
    </a>
  </li>
  <li>
    <a href="/business/wallet">
      <strong>Wallet</strong>
      <span>Créer un portefeuille dans la devise voulue et suivre son solde.</span>
    </a>
  </li>
  <li>
    <a href="/mobile-money/deposits-payouts">
      <strong>Mobile Money</strong>
      <span>Dépôts et retraits via PawaPay, côté collecte comme côté payout.</span>
    </a>
  </li>
  <li>
    <a href="/mobile-money/webhooks">
      <strong>Webhooks</strong>
      <span>Être notifié des événements en temps réel, sans polling.</span>
    </a>
  </li>
</ul>

## Deux façons d'appeler ces APIs

| Accès | Identifiant | Usage |
|---|---|---|
| **Clé API** | `Authorization: Bearer biz_live_...` / `biz_sandbox_...` | Intégration serveur-à-serveur — c'est ce que vous utiliserez en production |
| **Session dashboard** | Token de connexion humaine | Actions déclenchées depuis l'interface du dashboard marchand |

Les deux exécutent exactement les mêmes contrôleurs — le choix dépend de qui déclenche l'action (votre backend, ou un humain dans le dashboard).

## Conventions

### Montants

Tous les montants sont exprimés en **plus petite unité de la devise** (équivalent centimes), sous forme de chaîne (`string`) pour éviter toute perte de précision — jamais de `number` flottant.

```json
{ "amount": "50000", "currency_code": "USD" }
```

### Format des erreurs

```json
{ "message": "Description de l'erreur" }
```

### Codes de statut HTTP

| Code | Signification |
|---|---|
| `200` / `201` / `202` | Succès (`202` = traitement asynchrone lancé, ex. dépôt mobile money) |
| `400` | Requête invalide (règle métier non satisfaite) |
| `401` | Authentification absente ou invalide |
| `403` | Authentifié mais action refusée (statut du compte, permissions) |
| `404` | Ressource introuvable (ou n'appartenant pas à l'appelant — pas de fuite d'existence) |
| `409` | Conflit (ex. clé d'idempotence déjà utilisée avec un payload différent) |
| `422` | Validation échouée (format, bornes de montant, type de fichier...) |
| `429` | Trop de tentatives (verrouillage anti-bruteforce) |
| `503` | Service externe indisponible (PawaPay) |
